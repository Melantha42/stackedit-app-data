

## 优先级顺序计算
-   在C语言中，乘法和除法是**左结合**的，优先级相同
    
-   `a / b * c` 等价于 `(a / b) * c`，而不是 `a / (b * c)`

## 三目运算符
>条件 ? 表达式1 : 表达式2;
 
 ### 示例：
>int max = (a > b) ? a : b;
>bi'jiao
<!--stackedit_data:
eyJoaXN0b3J5IjpbNDgwNDI0NzkxLC0xMTMwNjMwMTk5XX0=
-->